{
  "name": "combine-reducers-react",
  "version": "1.0.0",
  "description": "A tiny package for combine reducers that mimic the actual combine reducers of redux  while using useReducers with contextApi",
  "main": "dist",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && npx babel src --out-dir dist"
  },
  "repository": {
    "type": "git",
    "url": "git+git@github.com:srikanth000/combine-reducers-react.git"
  },
  "keywords": [
    "combine-reducers",
    "ContextApi",
    "useReducer",
    "combine",
    "reducers",
    "react-hooks",
    "redux",
    "Context-provider"
  ],
  "author": "Srikanth Margam",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/srikanth000/combine-reducers-react/issues"
  },
  "homepage": "https://github.com/srikanth000/combine-reducers-react#readme",
  "devDependencies": {
    "babel-cli": "^6.26.0"
  },
  "dependencies": {
    "@babel/cli": "^7.12.0",
    "@babel/core": "^7.12.0",
    "@babel/preset-env": "^7.12.0"
  }
}
